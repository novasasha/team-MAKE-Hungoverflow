<form method="post" action="/<%=voteable.class.to_s.downcase + 's'%>/<%= voteable.id %>/vote" class="inline">
    <input type="hidden" name="points" value="<%=vote_value%>">
    <input type="submit" value="<%= vote_type %>">
</form>
