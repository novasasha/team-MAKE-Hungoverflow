<div class="question-main">
  <h2><%=@question.title%></h2>
  <article><%=@question.body%></article>
  <% @question.answers.each do |answer| %>
    <%= erb :"/answers/_show", locals: { answer: answer } %>
  <% end %>



  <!-- vote stuff -->
  <div class="vote">
    <%= erb :"_votes", locals:{voteable: @question, value: 1, label: "upvote"} %>
    <%= erb :"_votes", locals:{voteable: @question, value: 1, label: "downvote"} %>
  </div>
  <!-- vote stuff -->
</div>

